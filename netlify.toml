[build]
  command = "npm run build"
  publish = "dist/food-blog/browser"

[build.environment]
  NODE_VERSION = "24"

# Client-side routing for the Angular SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
